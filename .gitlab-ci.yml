#before_build:
    

stages:
    - prepare
    - build
    - test
    - deploy
    - update 

variables:
    GIT_SUBMODULE_STRATEGY: normal

cellframe-dashboard_windows_update:
    stage: prepare
    rules:
    - if: $UPDATE == "true"
  #    ref: master
    tags:
     - cellframe-dashboard
  # only:
  #   - master
    script: ~/scripts/update_version.sh

cellframe-dashboard_linux:
    stage: build
    tags:
     - cellframe-dashboard
    script: ~/production/integration/gitlab-CI/build_all_handler.sh cellframe-dashboard linux


cellframe-dashboard_windows:
   stage: build
   tags:
    - cellframe-dashboard
   script: ~/production-docker/integration/gitlab-CI/build_all_handler.sh cellframe-dashboard windows

# Test
# run unit-tests

# Deploy
# cp to builds folder
